# 🔄 Пересборка веб-контейнера

## ⚠️ Проблема:

Frontend все еще использует старый код с прямым подключением к порту контейнера вместо прокси через API.

В логах видно:
```
🌐 VNC URL: http://109.172.101.73:38322/vnc.html
```

Вместо:
```
🌐 VNC URL: http://109.172.101.73:5050/api/profile/23/browser/proxy?path=vnc.html
```

---

## ✅ Решение:

Нужно пересобрать веб-контейнер с новыми изменениями.

---

## 📋 Команды:

```bash
# 1. Перейти в директорию
cd /opt/mask-browser/infra

# 2. Пересобрать веб-контейнер
docker-compose up -d --build web

# 3. Проверить логи
docker logs maskbrowser-web --tail 50

# 4. Очистить кэш браузера и перезагрузить страницу
# (Ctrl+Shift+R или Cmd+Shift+R)
```

---

## 🔍 Проверка:

После пересборки в консоли браузера должно быть:
```
🌐 VNC URL: http://109.172.101.73:5050/api/profile/23/browser/proxy?path=vnc.html&autoconnect=true&resize=scale
```

А не:
```
🌐 VNC URL: http://109.172.101.73:38322/vnc.html
```

---

## ⚠️ Важно:

- Обязательно очистите кэш браузера после пересборки
- Используйте жесткую перезагрузку: Ctrl+Shift+R (Windows/Linux) или Cmd+Shift+R (Mac)
- Или откройте в режиме инкогнито для проверки

