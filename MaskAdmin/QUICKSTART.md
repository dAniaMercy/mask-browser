# MaskAdmin - Quick Start Guide

## 🚀 Быстрый старт за 5 минут

### Вариант 1: Docker Compose (самый простой)

```bash
# 1. Клонировать репозиторий
git clone https://github.com/yourusername/maskadmin.git
cd maskadmin

# 2. Запустить все сервисы
docker-compose up -d

# 3. Дождаться запуска (проверить логи)
docker-compose logs -f maskadmin

# 4. Открыть браузер
open http://localhost:5100
```

**Вход:**
- Email: `admin@maskbrowser.com`
- Password: `Admin123!`

### Вариант 2: Локальная разработка

```bash
# 1. Установить зависимости
sudo apt install -y postgresql redis-server

# 2. Создать базу данных
sudo -u postgres psql -c "CREATE DATABASE maskadmin;"
sudo -u postgres psql -c "CREATE USER maskadmin WITH PASSWORD 'maskadmin123';"
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE maskadmin TO maskadmin;"

# 3. Применить миграции
dotnet ef database update

# 4. Запустить приложение
dotnet run

# 5. Открыть браузер
open http://localhost:5100
```

## 📋 Основные разделы

### 1. Dashboard (Главная)
- **URL:** `/dashboard`
- **Назначение:** Общая статистика и мониторинг системы
- **Что показывает:**
  - Количество пользователей, профилей, серверов
  - Графики активности за последние дни
  - Статус серверных нод
  - Топ пользователи

### 2. Users (Пользователи)
- **URL:** `/users`
- **Возможности:**
  - Просмотр всех пользователей
  - Поиск и фильтрация
  - Редактирование информации
  - Управление балансом
  - Бан/разбан пользователей
  - Управление подписками

**Действия с пользователем:**
1. Нажмите на пользователя в списке
2. Откроется детальная страница
3. Доступные операции:
   - ✏️ Edit - редактировать
   - 💰 Adjust Balance - изменить баланс
   - 🔒 Ban - заблокировать
   - ❄️ Freeze - заморозить
   - 🗑️ Delete - удалить
   - 🔑 Reset Password - сбросить пароль
   - 💎 Manage Subscription - управление подпиской

### 3. Subscriptions (Подписки)
- **URL:** `/subscriptions`
- **Планы:**
  - Free: 1 профиль, $0/мес
  - Basic: 5 профилей, $10/мес
  - Pro: 20 профилей, $30/мес
  - Enterprise: 100+ профилей, $100/мес

**Назначить подписку:**
1. Откройте пользователя
2. Нажмите "Manage Subscription"
3. Выберите план
4. Укажите срок действия
5. Нажмите "Assign"

### 4. Servers (Серверы)
- **URL:** `/servers`
- **Что показывает:**
  - Список всех серверов
  - Статус (онлайн/оффлайн)
  - Загрузка CPU и RAM
  - Количество контейнеров

**Добавить новый сервер:**
1. Нажмите "Register New Server"
2. Укажите:
   - Name (например, "node-1")
   - IP Address (например, "192.168.1.101")
   - Max Containers (например, 1000)
3. Нажмите "Register"

### 5. Profiles (Профили)
- **URL:** `/profiles`
- **Возможности:**
  - Просмотр всех профилей
  - Фильтрация по пользователю/статусу
  - Запуск/остановка профилей
  - Удаление профилей

### 6. Payments (Платежи)
- **URL:** `/payments`
- **Что показывает:**
  - История всех платежей
  - Статистика доходов
  - Фильтрация по статусу/провайдеру

**Экспорт платежей:**
1. Настройте фильтры
2. Нажмите "Export"
3. Выберите формат (CSV или Excel)

### 7. Logs (Логи)
- **URL:** `/logs`
- **Категории:**
  - Authentication - входы, выходы
  - User Management - управление пользователями
  - Profile Management - управление профилями
  - Server Management - серверы
  - Payment Management - платежи
  - Security - безопасность
  - System - системные события

### 8. Settings (Настройки)
- **URL:** `/settings`
- **Разделы:**
  - General - общие настройки
  - Security - безопасность
  - Payment - платежные провайдеры
  - Server - настройки серверов
  - Email - настройка почты

## 🔍 Полезные команды

### Docker

```bash
# Просмотр логов
docker-compose logs -f maskadmin

# Перезапуск приложения
docker-compose restart maskadmin

# Остановка всех сервисов
docker-compose down

# Запуск с пересборкой
docker-compose up -d --build
```

### Database

```bash
# Создать миграцию
dotnet ef migrations add MigrationName

# Применить миграции
dotnet ef database update

# Откатить миграцию
dotnet ef database update PreviousMigrationName

# Удалить последнюю миграцию
dotnet ef migrations remove
```

### Logs

```bash
# Просмотр логов приложения
tail -f logs/maskadmin-*.log

# Просмотр логов PostgreSQL
tail -f /var/log/postgresql/postgresql-15-main.log

# Просмотр логов Redis
tail -f /var/log/redis/redis-server.log
```

## 🎯 Типичные сценарии

### Сценарий 1: Добавление нового пользователя

1. Users → Add New User
2. Заполните Username, Email, Password
3. Create
4. Manage Subscription → выберите план
5. Assign

### Сценарий 2: Блокировка пользователя

1. Users → найдите пользователя
2. Откройте детали
3. Ban → укажите причину
4. Confirm

### Сценарий 3: Изменение баланса

1. Users → найдите пользователя
2. Adjust Balance
3. Укажите сумму (положительную или отрицательную)
4. Укажите причину
5. Apply

### Сценарий 4: Мониторинг серверов

1. Servers → список всех серверов
2. Выберите сервер для деталей
3. Просмотрите метрики:
   - CPU Usage
   - Memory Usage
   - Active Containers
   - Network I/O

### Сценарий 5: Экспорт логов

1. Logs → выберите категорию
2. Настройте фильтры (дата, уровень)
3. Export → выберите формат
4. Скачайте файл

## 🛠️ Troubleshooting

### Проблема: Не могу войти

**Решение:**
```bash
# Сбросить пароль администратора через базу данных
docker exec -it maskadmin-postgres psql -U maskadmin -d maskadmin
UPDATE "Users" SET "PasswordHash" = '$2a$11$...' WHERE "Email" = 'admin@maskbrowser.com';
```

### Проблема: Приложение не запускается

**Решение:**
```bash
# 1. Проверить логи
docker-compose logs maskadmin

# 2. Проверить базу данных
docker exec -it maskadmin-postgres psql -U maskadmin -c "\dt"

# 3. Проверить Redis
docker exec -it maskadmin-redis redis-cli ping

# 4. Пересобрать и перезапустить
docker-compose down
docker-compose up -d --build
```

### Проблема: Медленная работа

**Решение:**
```bash
# 1. Проверить использование ресурсов
docker stats

# 2. Увеличить ресурсы Redis
# В docker-compose.yml добавить:
redis:
  command: redis-server --maxmemory 512mb --maxmemory-policy allkeys-lru

# 3. Очистить кэш
docker exec -it maskadmin-redis redis-cli FLUSHALL
```

## 📱 Мобильный доступ

MaskAdmin адаптивен и работает на мобильных устройствах:

- Планшеты: полный функционал
- Телефоны: упрощенная навигация
- Touch-friendly элементы управления

## 🔒 Безопасность

### Важные настройки для production:

1. **Измените пароль администратора**
2. **Смените JWT SecretKey** в appsettings.json
3. **Используйте HTTPS**
4. **Настройте Firewall:**
   ```bash
   sudo ufw allow 5100/tcp
   sudo ufw enable
   ```
5. **Ограничьте доступ к БД:**
   ```bash
   # В PostgreSQL pg_hba.conf
   host maskadmin maskadmin 127.0.0.1/32 md5
   ```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Посмотрите [README.md](README.md) для детальной документации
3. Создайте Issue на GitHub
4. Напишите в поддержку: support@maskbrowser.com

---

**Готово! Теперь вы можете начать работать с MaskAdmin 🚀**
