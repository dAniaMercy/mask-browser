# 🔒 Решение с проксированием через API

## ✅ Преимущества:

1. **Безопасность**: Не нужно открывать порты контейнеров напрямую
2. **Авторизация**: Все запросы проходят через API с проверкой JWT токена
3. **Централизованное управление**: Легче контролировать доступ и логировать
4. **CORS**: Нет проблем с CORS, так как все идет через один домен

---

## 📋 Как это работает:

1. **Frontend** запрашивает `/api/profile/{id}/browser/proxy?path=vnc.html`
2. **API** проверяет авторизацию и права доступа к профилю
3. **API** проксирует HTTP запрос к контейнеру профиля
4. **API** модифицирует HTML noVNC, заменяя WebSocket URL на прокси endpoint
5. **noVNC клиент** подключается к `/api/profile/{id}/browser/ws` (WebSocket прокси)
6. **API** проксирует WebSocket соединение к websockify в контейнере

---

## 🔧 Что было изменено:

### 1. `server/Controllers/BrowserProxyController.cs` (новый)
- `GET /api/profile/{id}/browser/proxy` - проксирование HTTP запросов
- `GET /api/profile/{id}/browser/ws` - проксирование WebSocket соединений

### 2. `client-web-nextjs/app/dashboard/profile/[id]/browser/page.tsx`
- Изменен URL с прямого подключения на прокси через API

### 3. `server/Program.cs`
- Добавлен `HttpClientFactory` для проксирования запросов

---

## 🚀 Команды для применения:

```bash
# 1. Пересобрать API контейнер
cd /opt/mask-browser/infra
docker-compose up -d --build api

# 2. Проверить логи
docker logs maskbrowser-api --tail 50

# 3. Создать новый профиль и проверить работу
```

---

## ✅ Ожидаемый результат:

- ✅ Браузер загружается через API прокси
- ✅ WebSocket соединение работает через API
- ✅ Нет необходимости открывать порты контейнеров
- ✅ Все запросы авторизованы через JWT

---

## 🔍 Проверка:

1. Откройте браузер профиля
2. Проверьте Network tab в DevTools:
   - Должен быть запрос к `/api/profile/{id}/browser/proxy`
   - Должно быть WebSocket соединение к `/api/profile/{id}/browser/ws`
3. Проверьте логи API:
   ```bash
   docker logs maskbrowser-api --tail 100 | grep -i proxy
   ```

---

## ⚠️ Важно:

- Порт контейнера все еще пробрасывается на хост для внутреннего доступа API
- Но внешний доступ идет только через API с авторизацией
- Это намного безопаснее, чем открывать порты напрямую

