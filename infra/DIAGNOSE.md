# 🔍 Диагностика проблемы с браузером

## ❌ Ошибка: `ERR_CONNECTION_REFUSED` на порту 6080

Это означает, что websockify не запущен или не слушает на порту 6080.

---

## 📋 Шаги диагностики:

### 1. Проверить, что образ пересобран

```bash
# Проверить, когда был собран образ
docker images maskbrowser/browser:latest

# Если образ старый, пересобрать:
cd /opt/mask-browser/infra
docker build -t maskbrowser/browser:latest -f Dockerfile.browser .
```

### 2. Проверить логи контейнера профиля

```bash
# Найти контейнер профиля
docker ps | grep maskbrowser-profile

# Проверить логи (замените <ID> на ID контейнера)
docker logs maskbrowser-profile-18 --tail 100
```

**Что искать в логах:**
- ✅ `✅ VNC server is ready on port 5900`
- ✅ `✅ Starting websockify with noVNC web interface on port 6080...`
- ❌ Ошибки websockify
- ❌ Ошибки supervisor

### 3. Проверить статус supervisor

```bash
docker exec maskbrowser-profile-18 supervisorctl status
```

**Ожидаемый результат:**
```
selenium-node                    RUNNING
vnc                             RUNNING
websockify                      RUNNING
```

**Если websockify не запущен:**
```bash
# Перезапустить websockify
docker exec maskbrowser-profile-18 supervisorctl restart websockify

# Проверить логи
docker exec maskbrowser-profile-18 cat /var/log/websockify.err.log
docker exec maskbrowser-profile-18 cat /var/log/websockify.out.log
```

### 4. Проверить порты

```bash
# Проверить, что порты слушаются внутри контейнера
docker exec maskbrowser-profile-18 netstat -tlnp | grep -E ":(5900|6080)"

# Должно показать:
# tcp  0  0  0.0.0.0:5900  LISTEN  (VNC)
# tcp  0  0  0.0.0.0:6080  LISTEN  (websockify)
```

### 5. Проверить проброс портов

```bash
# Проверить, какие порты проброшены на хост
docker port maskbrowser-profile-18

# Должно показать:
# 6080/tcp -> 0.0.0.0:15653
```

### 6. Проверить доступность с хоста

```bash
# Проверить доступность порта (замените <PORT> на порт из docker port)
curl -I http://localhost:<PORT>/vnc.html

# Или с IP сервера
curl -I http://109.172.101.73:<PORT>/vnc.html
```

---

## 🔧 Решения проблем:

### Проблема 1: websockify не запускается через supervisor

**Причина:** Supervisor не загрузил конфигурацию или права доступа неправильные.

**Решение:**
```bash
# Пересобрать образ с правильными правами
cd /opt/mask-browser/infra
docker build -t maskbrowser/browser:latest -f Dockerfile.browser .

# Пересоздать контейнер профиля
# (остановить и удалить старый, создать новый через веб-интерфейс)
```

### Проблема 2: VNC не готов к моменту запуска websockify

**Причина:** websockify запускается раньше, чем VNC.

**Решение:** Скрипт `start-websockify.sh` уже ждет VNC до 60 секунд. Если проблема сохраняется, увеличьте время ожидания.

### Проблема 3: Образ не пересобран

**Причина:** Используется старый образ без websockify.conf.

**Решение:**
```bash
# Принудительно пересобрать образ
cd /opt/mask-browser/infra
docker build --no-cache -t maskbrowser/browser:latest -f Dockerfile.browser .
```

### Проблема 4: noVNC не найден

**Причина:** noVNC не установлен в образе.

**Решение:**
```bash
# Проверить наличие noVNC в контейнере
docker exec maskbrowser-profile-18 ls -la /usr/share/novnc/

# Если нет, пересобрать образ
docker build -t maskbrowser/browser:latest -f Dockerfile.browser .
```

---

## 🚀 Быстрое решение (если ничего не помогло):

```bash
# 1. Остановить все контейнеры профилей
docker ps | grep maskbrowser-profile | awk '{print $1}' | xargs -r docker stop

# 2. Удалить все контейнеры профилей
docker ps -a | grep maskbrowser-profile | awk '{print $1}' | xargs -r docker rm

# 3. Пересобрать образ БЕЗ кэша
cd /opt/mask-browser/infra
docker build --no-cache -t maskbrowser/browser:latest -f Dockerfile.browser .

# 4. Проверить, что образ собран
docker images | grep maskbrowser/browser

# 5. Создать новый профиль через веб-интерфейс

# 6. Проверить логи нового контейнера
docker logs maskbrowser-profile-<NEW_ID> --tail 100
```

---

## 📊 Использование скрипта диагностики:

```bash
# Сделать скрипт исполняемым
chmod +x infra/CHECK-CONTAINER.sh

# Запустить диагностику
./infra/CHECK-CONTAINER.sh maskbrowser-profile-18
```

---

## ✅ Ожидаемый результат после исправления:

1. ✅ Supervisor запущен
2. ✅ VNC работает на порту 5900
3. ✅ websockify работает на порту 6080
4. ✅ noVNC доступен по `http://<IP>:<PORT>/vnc.html`
5. ✅ Браузер загружается в iframe

