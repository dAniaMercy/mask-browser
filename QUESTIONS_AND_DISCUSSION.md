# Вопросы по реализации, нововведениям и исправлениям

## 🎯 Основные вопросы

### 1. **Маршрутизация nginx**

✅ **Сделано:**
- Созданы конфигурации для всех трех доменов
- Убраны конфликты с `default_server`
- Создан скрипт автоматической настройки

❓ **Вопросы:**
1. **wbmoneyback.ru** - какая конфигурация нужна для этого домена?
   - Какой порт/сервис должен обслуживать этот домен?
   - Нужен ли прокси или статические файлы?
   - Есть ли уже существующая конфигурация, которую нужно сохранить?

2. **Порядок загрузки конфигов** - использовать префиксы (00-, 01-, 99-)?
   - Текущее решение: префиксы для правильного порядка
   - Альтернатива: убрать все `default_server` из других конфигов

3. **SSL сертификаты** - все домены уже имеют сертификаты?
   - Если нет, нужно ли автоматически получать через certbot?

---

### 2. **API создания профилей**

✅ **Текущая реализация:**
- `ProfileService.CreateProfileAsync()` - создает профиль в БД
- `ProfileService.StartProfileAsync()` - создает Docker контейнер
- `DockerService.CreateBrowserContainerAsync()` - управляет контейнерами

❓ **Вопросы:**

1. **Валидация конфигурации профиля:**
   - Нужна ли валидация UserAgent на стороне сервера?
   - Какие ограничения на ScreenResolution (разрешенные значения)?
   - Нужна ли валидация Timezone (список разрешенных)?

2. **Обработка ошибок создания контейнера:**
   - Что делать, если контейнер создался, но не запустился?
   - Нужна ли автоматическая очистка "висящих" контейнеров?
   - Как обрабатывать ситуацию, когда Docker недоступен?

3. **Лимиты ресурсов:**
   - Текущий лимит: 512MB RAM, 0.5 CPU на контейнер
   - Нужно ли сделать это настраиваемым через подписку?
   - Нужны ли разные лимиты для разных типов подписок?

4. **Персистентность данных профиля:**
   - Текущая реализация: bind mount `/var/lib/maskbrowser/profiles/{profileId}`
   - Нужна ли синхронизация данных между нодами?
   - Что делать при удалении профиля - удалять данные сразу или через N дней?

5. **Мониторинг контейнеров:**
   - Есть `ContainerMonitorJob` - что он делает?
   - Нужны ли health checks для контейнеров?
   - Как обрабатывать "мертвые" контейнеры (запущены, но не отвечают)?

---

### 3. **Безопасность**

❓ **Вопросы:**

1. **Изоляция контейнеров:**
   - Нужна ли изоляция сетей между контейнерами разных пользователей?
   - Использовать ли Docker network isolation?
   - Нужны ли firewall правила между контейнерами?

2. **Rate limiting:**
   - Текущее состояние: нет rate limiting на API
   - Нужен ли rate limiting? Какие лимиты?
   - По IP или по пользователю?

3. **Валидация входных данных:**
   - Нужна ли валидация имени профиля (длина, символы)?
   - Нужна ли санитизация конфигурации профиля?
   - Защита от SQL injection (используется EF Core, но нужно проверить)?

4. **Логирование:**
   - Какие данные логировать? (не логировать пароли, токены)
   - Нужна ли маскировка чувствительных данных в логах?
   - Где хранить логи? (Loki уже настроен)

---

### 4. **Производительность и масштабирование**

❓ **Вопросы:**

1. **Кэширование:**
   - Нужно ли кэшировать список профилей пользователя?
   - Кэшировать ли информацию о нодах?
   - Какой TTL для кэша?

2. **Балансировка нагрузки:**
   - Текущая логика: выбор ноды по количеству контейнеров
   - Нужна ли более сложная логика (по CPU, памяти, latency)?
   - Как обрабатывать отказ ноды? (автоматический failover?)

3. **Масштабирование:**
   - Как добавлять новые ноды? (вручную или автоматически?)
   - Нужна ли автоматическая регистрация нод?
   - Как синхронизировать состояние между нодами?

4. **База данных:**
   - Используется PostgreSQL - нужна ли репликация?
   - Нужна ли оптимизация запросов (индексы, проекции)?
   - Нужна ли пагинация для списка профилей?

---

### 5. **Профили в виртуальных машинах**

❓ **Вопросы для обсуждения:**

1. **Требования:**
   - Зачем нужны VM вместо Docker?
   - Какие конкретные требования к изоляции?
   - Есть ли требования по безопасности, которые Docker не может обеспечить?

2. **Ресурсы:**
   - Сколько RAM/CPU доступно на сервере?
   - Сколько профилей планируется одновременно?
   - Какой бюджет на ресурсы?

3. **Производительность:**
   - Критично ли время запуска профиля?
   - Docker: 5-10 секунд
   - VM: 30-60 секунд

4. **Управление:**
   - Кто будет управлять VM? (нужны навыки работы с гипервизором)
   - Нужна ли автоматизация создания/удаления VM?
   - Как мониторить состояние VM?

5. **Гибридный подход:**
   - Docker для обычных пользователей
   - VM для премиум-пользователей
   - Выбор на основе подписки

6. **Технологии:**
   - Какой гипервизор предпочтителен? (KVM, VMware, Hyper-V)
   - Или использовать готовые решения? (Firecracker, gVisor, Kata Containers)

---

## 🔧 Предложения по исправлениям

### Критичные исправления

1. **Пустые catch блоки** (DockerService.cs)
   - ❌ Текущее: `catch { }`
   - ✅ Нужно: логировать ошибки

2. **Валидация портов** (DockerService.cs)
   - ❌ Текущее: случайный порт без проверки
   - ✅ Нужно: проверка доступности порта

3. **CORS настройки** (Program.cs)
   - ❌ Текущее: слишком широкие разрешения
   - ✅ Нужно: конкретные origins и методы

4. **Rate limiting** (отсутствует)
   - ❌ Текущее: нет защиты от злоупотреблений
   - ✅ Нужно: добавить middleware

### Важные улучшения

1. **Транзакции БД** (ProfileService.cs)
   - Добавить транзакции для атомарности операций

2. **Health checks** (DockerService.cs)
   - Проверка состояния контейнеров перед использованием

3. **Метрики Prometheus**
   - Добавить метрики для мониторинга

4. **Retry логика**
   - Повторные попытки при временных сбоях Docker

---

## 📋 Чеклист для проверки API

### Создание профиля
- [ ] Валидация имени профиля
- [ ] Валидация конфигурации
- [ ] Проверка лимитов подписки
- [ ] Создание записи в БД
- [ ] Обработка ошибок

### Запуск профиля
- [ ] Проверка существования профиля
- [ ] Проверка статуса профиля
- [ ] Выбор ноды
- [ ] Создание контейнера
- [ ] Получение порта
- [ ] Обновление статуса
- [ ] Обработка ошибок и откат

### Остановка профиля
- [ ] Остановка контейнера
- [ ] Обновление счетчиков ноды
- [ ] Обновление статуса
- [ ] Обработка ошибок

### Удаление профиля
- [ ] Остановка контейнера (если запущен)
- [ ] Удаление контейнера
- [ ] Удаление данных профиля
- [ ] Удаление записи из БД

---

## 🎯 Следующие шаги

1. **Ответить на вопросы выше** - чтобы понять требования
2. **Исправить критичные проблемы** - пустые catch, валидация портов
3. **Добавить rate limiting** - защита API
4. **Улучшить CORS** - безопасность
5. **Добавить метрики** - мониторинг
6. **Принять решение по VM** - нужны ли VM профили?

---

## 📞 Контакты для обсуждения

После ответа на вопросы я смогу:
- Реализовать недостающие функции
- Исправить найденные проблемы
- Оптимизировать производительность
- Добавить необходимую безопасность
- Реализовать VM профили (если решите)

---

**Дата:** $(date)
**Статус:** Ожидает ответов на вопросы
