# Настройка системы депозитов

## Обзор

Система депозитов состоит из:
1. **Server API** (ASP.NET Core) - основной API с эндпоинтами для депозитов
2. **CryptoBot Listener** (Python) - микросервис для обработки платежей через Telegram
3. **Database** - PostgreSQL с таблицами `deposit_requests` и `payment_methods`

## Быстрый старт

### 1. Применение миграций БД

```bash
cd server
dotnet ef database update
```

Или миграции применятся автоматически при запуске API через Docker.

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта или в `infra/`:

```env
# Telegram для CryptoBot Listener
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
TELEGRAM_PHONE=+7xxxxxxxxxx

# Webhook секрет (должен совпадать в обоих сервисах)
WEBHOOK_SECRET=your_super_secret_key_here

# Опционально: CryptoBot API Token
CRYPTOBOT_API_TOKEN=optional_token
```

### 3. Получение Telegram API credentials

1. Перейдите на https://my.telegram.org/apps
2. Войдите с вашим номером телефона
3. Создайте новое приложение
4. Скопируйте `api_id` и `api_hash`

### 4. Запуск через Docker Compose

```bash
cd infra
docker-compose up -d
```

Сервис `cryptobot-listener` запустится автоматически вместе с остальными сервисами.

### 5. Первый запуск CryptoBot Listener

При первом запуске Telethon запросит код подтверждения из SMS:

```bash
docker-compose logs -f cryptobot-listener
```

Введите код из SMS, когда появится запрос. Сессия сохранится в volume `cryptobot_sessions`.

## Создание метода оплаты

После запуска системы создайте метод оплаты через админ-панель или напрямую в БД:

```sql
INSERT INTO payment_methods (
    name, display_name, description, processor_type, processor_config,
    min_amount, max_amount, currency, is_enabled, sort_order
) VALUES (
    'CryptoBot USDT',
    '💎 Пополнить через Telegram',
    'Моментальное пополнение через CryptoBot в Telegram',
    'cryptobot',
    '{"bot_username": "@CryptoBot", "supported_currencies": ["USDT", "TON"]}',
    1.00,
    10000.00,
    'USDT',
    true,
    0
);
```

## Тестирование

### 1. Создание тестовой заявки через API

```bash
curl -X POST http://localhost:5050/api/deposit/create \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "paymentMethodId": 1,
    "amount": 10.00
  }'
```

Ответ:
```json
{
  "depositId": 1,
  "paymentCode": "MASK-A7B3X2",
  "expiresAt": "2025-12-08T13:30:00Z",
  "instructions": "...",
  "qrCodeUrl": null,
  "redirectUrl": "https://t.me/CryptoBot",
  "expectedAmount": 10.00,
  "currency": "USDT"
}
```

### 2. Тестирование платежа

1. Откройте @CryptoBot в Telegram
2. Отправьте любую сумму на ваш кошелек
3. В комментарии к платежу укажите код `MASK-A7B3X2`
4. CryptoBot Listener автоматически обработает платеж

### 3. Проверка статуса

```bash
curl http://localhost:5050/api/deposit/1/status \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"
```

## Мониторинг

### Логи CryptoBot Listener

```bash
docker-compose logs -f cryptobot-listener
```

### Проверка статуса сервиса

```bash
docker-compose ps cryptobot-listener
```

### Healthcheck

Сервис имеет healthcheck, который проверяет, что процесс запущен.

## Troubleshooting

### CryptoBot Listener не запускается

1. **Проверьте переменные окружения:**
   ```bash
   docker-compose exec cryptobot-listener env | grep TELEGRAM
   ```

2. **Проверьте логи:**
   ```bash
   docker-compose logs cryptobot-listener
   ```

3. **Убедитесь, что БД доступна:**
   ```bash
   docker-compose exec cryptobot-listener ping postgres
   ```

### Платежи не обрабатываются

1. **Проверьте формат кода:** должен быть `MASK-XXXXXX` (6 символов после дефиса)
2. **Проверьте, что код указан в комментарии к платежу**
3. **Убедитесь, что заявка не просрочена** (по умолчанию 30 минут)
4. **Проверьте логи на ошибки парсинга**

### Webhook не отправляется

1. **Проверьте доступность API:**
   ```bash
   curl http://api:8080/health
   ```

2. **Убедитесь, что `WEBHOOK_SECRET` совпадает** в обоих сервисах

3. **Проверьте логи API на наличие webhook запросов:**
   ```bash
   docker-compose logs api | grep webhook
   ```

### Сессия Telegram не сохраняется

1. **Проверьте volume:**
   ```bash
   docker volume inspect maskbrowser_old_cryptobot_sessions
   ```

2. **Убедитесь, что директория существует:**
   ```bash
   docker-compose exec cryptobot-listener ls -la /app/sessions
   ```

## Архитектура

```
┌─────────────┐
│   User      │
│  (Frontend) │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────────┐
│  Server API │─────▶│ Deposit Request  │
│ (ASP.NET)   │      │   (PostgreSQL)   │
└──────┬──────┘      └──────────────────┘
       │
       │ Webhook
       ▼
┌─────────────────────┐
│ CryptoBot Listener  │
│    (Python)         │
└──────┬──────────────┘
       │
       ├──▶ Telegram (@CryptoBot)
       │
       ├──▶ Redis (deduplication)
       │
       └──▶ PostgreSQL (update balance)
```

## Безопасность

1. **Webhook подпись:** Все webhook запросы подписываются HMAC SHA256
2. **Дедупликация:** Redis предотвращает повторную обработку платежей
3. **Валидация:** Проверка срока действия заявок и принадлежности пользователю
4. **Транзакции:** Все операции с балансом выполняются в транзакциях БД

## Дополнительные ресурсы

- [Спецификация системы депозитов](./docs/MASK_BROWSER_DEPOSIT_SYSTEM_SPEC.md)
- [README CryptoBot Listener](./cryptobot-listener/README.md)
- [API документация](./docs/api-reference.md)
