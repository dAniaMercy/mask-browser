global:
  resolve_timeout: 5m
  # Telegram configuration
  # telegram_api_url: 'https://api.telegram.org'
  # telegram_bot_token: 'YOUR_BOT_TOKEN'
  # telegram_chat_id: 'YOUR_CHAT_ID'

route:
  group_by: ['alertname', 'cluster', 'service']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'
  routes:
    - match:
        severity: critical
      receiver: 'critical'
    - match:
        severity: warning
      receiver: 'warning'

receivers:
  - name: 'default'
    # webhook_configs:
    #   - url: 'http://api:5050/api/alerts'

  - name: 'critical'
    # telegram_configs:
    #   - bot_token: 'YOUR_BOT_TOKEN'
    #     chat_id: 'YOUR_CHAT_ID'
    #     message: '{{ .CommonAnnotations.description }}'

  - name: 'warning'
    # webhook_configs:
    #   - url: 'http://api:8080/api/alerts'

